fact = \(x) -> if x <= 1, 1, x * fact(x - 1);

forloop = \(from|to|f) -> {
    (if from <= to, \() -> {
        f(from);
        forloop(from + 1, to, f);
    }, \() -> {})();
};

main = \() -> {
    forloop(0, 50, \(x) -> println(x + "! = " + fact(x)));
};

main();